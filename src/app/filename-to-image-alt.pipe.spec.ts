import { FilenameToImageAltPipe } from './filename-to-image-alt.pipe';

describe('FilenameToImageAltPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameToImageAltPipe();
    expect(pipe).toBeTruthy();
  });
});
